% ============================================
% Method
% ============================================




\subsection{A Subsection Sample}
Please note that the first paragraph of a section or subsection is
not indented. The first paragraph that follows a table, figure,
equation etc. does not need an indent, either.
\begin{equation}
    \mathbf{H} = \text{BERT}(\mathbf{x}) \in \mathbf{R}^{n \times d}
    \label{eq:encoder}
\end{equation}



\subsection{A Subsection Sample}
Please note that the first paragraph of a section or subsection is
not indented. The first paragraph that follows a table, figure,
equation etc. does not need an indent, either.
\begin{equation}
    \mathbf{Z} = \text{CrossAttn}(\mathbf{H}, \mathbf{H}) = \text{softmax}\left(\frac{\mathbf{H} \mathbf{W}_Q (\mathbf{H} \mathbf{W}_K)^\top}{\sqrt{d_k}}\right) \mathbf{H} \mathbf{W}_V
    \label{eq:crossattn}
\end{equation}


\subsection{A Subsection Sample}
Please note that the first paragraph of a section or subsection is
not indented. The first paragraph that follows a table, figure,
equation etc. does not need an indent, either.
\begin{equation}
    \mathcal{L} = -\frac{1}{N} \sum_{i=1}^{N} \sum_{c=1}^{C} y_{i,c} \log(\hat{y}_{i,c})
    \label{eq:loss}
\end{equation}